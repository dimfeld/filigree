{% import "sql_macros.tera" as macros %}
SELECT
  {{ fields
    | filter(attribute='owner_read', value=true)
    | map(attribute='sql_full_name')
    | join(sep=',\n') }},
FROM
  {{ table }}
{% if not global %}
WHERE
    team = $1
{% endif %}

